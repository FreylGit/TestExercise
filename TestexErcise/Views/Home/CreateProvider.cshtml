@model Provider
@{
    ViewData["Title"] = "Создание поставщика";
}
<div class="container">
    <div class="row justify-content-center">
        <div class="col-4">
            <form method="post" asp-action="CreateProvider" class="text-ce">
                <div class="form-group">
                    <label>Имя :</label><input type="text" asp-for="Name" class="form-control"  />
                </div>
                <button class="btn btn-secondary mt-4">Добавить</button>
            </form>
        </div>
    </div>
</div>
